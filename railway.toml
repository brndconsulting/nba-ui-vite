[build]
builder = "nixpacks"
buildCommand = "rm -rf dist && pnpm install --frozen-lockfile && pnpm build"

[deploy]
startCommand = "pnpm dlx serve -s dist/public -l $PORT --no-cache"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
