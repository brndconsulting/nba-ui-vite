[build]
builder = "nixpacks"
buildCommand = "rm -rf dist node_modules .pnpm-store && pnpm install --frozen-lockfile && pnpm build"

[deploy]
startCommand = "pnpm dlx serve -s dist/public -l $PORT --no-cache"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
